
# Dictionaries

Dictionaries are sets of `key:value` pairs.

- Keys are unique
- Keys are typically strings or ints (immutable types)
- Values correspond to each key.
- Format is `key:value`

Note: Since they are sets, we cannot assume order.
However, python 3.7+ maintains insertion order.
